# Configuration file for the SynchronizedFactory environment
#
# Author: Kajetan Frąckowiak
# Date: 2026
#
# Description: This file contains hiperparameters needed for SynchornizedFactory environment

# Logic Environment parameters 
name: "synchronized"
max_steps: 200
grid_size: 10
n_agents: 3

# Dimensions for neural networks
observation_dimension: 8  # [self_x, self_y, crate_x, crate_y, goal_x, goal_y, rm_state, step]
action_dimension: 5       # [stay, left, right, down, up]
state_dimension: 24       # 3 agents * 8 obs

# Metadata
supports_vmas: False
supports_smac: False
supports_logic_env: True